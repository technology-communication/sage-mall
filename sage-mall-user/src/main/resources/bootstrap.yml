server:
  port: 8080
spring:
  cloud:
    nacos:
      discovery:
        #服务注册
        register-enabled: true
        server-addr: @nacos.address@
        weight: 1
      config:
        #配置中心
        server-addr: @nacos.address@
        file-extension: yaml

