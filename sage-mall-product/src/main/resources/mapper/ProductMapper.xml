<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dls.product.mapper.ProductMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.dls.product.entity.mysql.Product">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="price" property="price" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, price
    </sql>

    <insert id="insert" parameterType="com.dls.product.entity.mysql.Product">
        insert into spu_product(id,name,price) values(#{id},#{name},#{price})
    </insert>

</mapper>
